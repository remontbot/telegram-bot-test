# ⚡ Быстрый старт для новой сессии

## 🎯 Копируй это в начало новой беседы:

```
ПРОЕКТ: Telegram Bot "Поиск мастеров"
Репозиторий: remontbot/telegram-bot-test

Стек: Python 3.11 + python-telegram-bot v21.0 + PostgreSQL
Файлы: handlers.py (10k), db.py (6k), bot.py

Git workflow:
- Main branch: main
- Рабочая ветка: claude/telegram-master-finder-bot-XXXXX
- ВСЕГДА работаю в claude/* ветке
- После каждого push → отправляю ссылку на PR

Критичные правила:
✅ Совместимость SQLite + PostgreSQL
✅ async/await для handlers
✅ Smart notifications (delete old + send new для звука)
✅ Бейджи в меню (🔴 X новых)
❌ НЕ пушить в main напрямую
❌ НЕ добавлять features без запроса

Подробности в PROJECT_BRIEF.md 📚
```

---

## 🔄 Workflow для каждой задачи:

### 1️⃣ ПОНИМАНИЕ ЗАДАЧИ
```bash
# Прочитай контекст
git status
git log --oneline -3
```

### 2️⃣ РАЗРАБОТКА
```bash
# Используй grep/read для поиска нужного кода
# Вноси изменения в handlers.py, db.py, bot.py
# Тестируй логику
```

### 3️⃣ КОММИТ
```bash
git add handlers.py db.py bot.py
git commit -m "$(cat <<'EOF'
🎯 ЗАГОЛОВОК

Изменения:
1. Что сделано
2. Что исправлено
3. Что добавлено

Файлы: handlers.py, db.py, bot.py
EOF
)"
```

### 4️⃣ PUSH
```bash
git push -u origin claude/telegram-master-finder-bot-XXXXX
```

### 5️⃣ PR ССЫЛКА
```
https://github.com/remontbot/telegram-bot-test/compare/main...claude/telegram-master-finder-bot-XXXXX
```

**⚠️ ВСЕГДА отправляй эту ссылку пользователю!**

---

## 📋 Чек-лист перед коммитом:

- [ ] Код протестирован логически
- [ ] Совместимость PostgreSQL + SQLite проверена
- [ ] Async/await используется правильно
- [ ] Логирование добавлено где нужно
- [ ] Коммит-сообщение понятное и подробное
- [ ] Ветка правильная (claude/*)
- [ ] Ссылка на PR готова

---

## 🚨 Частые ошибки:

❌ **Push в main** → Всегда в claude/* ветку!
❌ **Забыл ссылку на PR** → Отправляй ВСЕГДА!
❌ **PostgreSQL vs SQLite** → Проверяй синтаксис!
❌ **Edit вместо Delete+Send** → Уведомления без звука!
❌ **Sync вместо Async** → python-telegram-bot v21+ только async!

---

## 📞 Если что-то непонятно:

1. Открой `PROJECT_BRIEF.md` - там всё детально
2. Поищи похожий код: `grep -n "паттерн" handlers.py`
3. Проверь последние коммиты: `git log --oneline -10`
4. Спроси пользователя

---

**Удачной работы!** 🚀
