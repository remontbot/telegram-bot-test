# 🤖 Telegram Bot: Сервис поиска мастеров

## 📋 О проекте

**Telegram бот для связи заказчиков и мастеров** (сантехники, электрики, ремонтники и т.д.)

### Основной функционал:
- 👤 **Заказчики**: Создают заказы → Получают отклики от мастеров → Выбирают мастера → Общаются в чате
- 🔧 **Мастера**: Просматривают заказы → Откликаются → Выполняют работу → Получают отзывы и фото в портфолио
- 💬 **Чаты**: Встроенная система общения между заказчиком и мастером
- ⭐ **Отзывы**: Взаимные отзывы после завершения работы
- 📸 **Портфолио**: Фото работ с подтверждением от клиентов
- 🔔 **Уведомления**: Smart-система с бейджами и звуковыми оповещениями

---

## 🛠 Технический стек

- **Python 3.11+** - основной язык
- **python-telegram-bot v21.0+** - async/await библиотека для Telegram Bot API
- **PostgreSQL** (production на Railway) / **SQLite** (local dev)
- **Railway** - хостинг и база данных

### Основные файлы:
```
├── bot.py              # Entry point, регистрация обработчиков
├── handlers.py         # Все обработчики событий (~10k строк)
├── db.py              # Работа с БД (~6k строк)
├── config.py          # Конфигурация
└── callbacks.py       # Вспомогательные callback функции
```

---

## 🌿 Git Workflow

### Ветки:

**Main branch:** `main` (основная ветка, защищена)

**Рабочие ветки:** `claude/telegram-master-finder-bot-XXXXX`
- ✅ ВСЕГДА работай в ветке с префиксом `claude/`
- ✅ Суффикс должен совпадать с session ID
- ❌ НИКОГДА не пуши напрямую в `main`

### Процесс работы:

1. **Разработка** на ветке `claude/telegram-master-finder-bot-XXXXX`
2. **Коммит** с понятным описанием на русском языке
3. **Push** в эту же ветку
4. **Создание PR** (Pull Request) в `main`
5. **Отправка ссылки на PR** пользователю

### Шаблон ссылки на PR:
```
https://github.com/remontbot/telegram-bot-test/compare/main...claude/telegram-master-finder-bot-XXXXX
```

### Правила коммитов:

```bash
git add файлы
git commit -m "$(cat <<'EOF'
🎯 ЗАГОЛОВОК: Краткое описание изменений

Детальное описание что сделано:
1. Первое изменение
2. Второе изменение
3. Третье изменение

Измененные файлы:
- handlers.py (строки): что изменено
- db.py (строки): что изменено
- bot.py: что изменено

🐛 Исправленные баги / ✨ Новые функции
EOF
)"
git push -u origin claude/telegram-master-finder-bot-XXXXX
```

### Push retry при ошибках:
- При network errors: retry до 4 раз с exponential backoff (2s, 4s, 8s, 16s)
- Используй: `git push -u origin <branch-name>`

---

## 🔑 Важные особенности кодовой базы

### 1. База данных

**Двойная совместимость:** SQLite (dev) + PostgreSQL (prod)

```python
if USE_POSTGRES:
    cursor.execute("SELECT * FROM table WHERE id = %s", (id,))
else:
    cursor.execute("SELECT * FROM table WHERE id = ?", (id,))
```

**Важно:**
- PostgreSQL строгий к типам: `verified = TRUE` (не `= 1`)
- SQLite возвращает `tuple`, PostgreSQL возвращает `dict`
- Всегда используй `dict(row)` для преобразования

### 2. Telegram Bot (async/await)

```python
async def handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    # ...
```

**Важные паттерны:**
- `context.user_data` - временное хранилище пользователя
- `db.set_active_chat()` / `db.get_active_chat()` - активный чат в БД
- `safe_edit_message()` - безопасное редактирование
- `InlineKeyboardMarkup` - кнопки под сообщениями

### 3. Уведомления (Smart Notifications)

**КРИТИЧНО:**
- Удаляем старое уведомление: `bot.delete_message()`
- Отправляем новое: `bot.send_message()` (всегда со звуком!)
- Обновление через `edit_message_text()` НЕ издает звук!

### 4. Меню с бейджами

```python
unread_count = notification.get('available_orders_count', 0)
button_text = "📋 Заказы"
if unread_count > 0:
    button_text = f"📋 Заказы 🔴 {unread_count}"
```

---

## 📊 Структура БД (основные таблицы)

```sql
users               -- Пользователи (telegram_id, role)
workers             -- Профили мастеров
clients             -- Профили заказчиков
orders              -- Заказы
bids                -- Отклики мастеров на заказы
chats               -- Чаты между клиентом и мастером
chat_messages       -- Сообщения в чатах
reviews             -- Отзывы
completed_work_photos  -- Фото завершенных работ
declined_orders     -- Отказы мастеров от заказов (НОВОЕ)
worker_notifications   -- Уведомления для мастеров
client_notifications   -- Уведомления для клиентов
active_chats        -- Активные чаты пользователей
```

---

## 🎯 Типичные задачи

### 1. Добавление новой функции

1. Создай функцию-обработчик в `handlers.py`
2. Добавь функции БД в `db.py` (если нужно)
3. Зарегистрируй обработчик в `bot.py`
4. Протестируй локально
5. Коммит → Push → PR

### 2. Исправление бага

1. Найди проблемное место через grep/read
2. Исправь код
3. Проверь совместимость SQLite + PostgreSQL
4. Коммит с описанием бага и решения
5. Push → PR

### 3. Изменение БД

1. Создай/измени таблицу в `db.py` (init_db)
2. Добавь функции для работы с таблицей
3. Обнови handlers.py для использования новых функций
4. ВАЖНО: PostgreSQL миграция происходит автоматически при первом запуске

---

## ⚠️ Критичные правила

### GIT:
- ✅ Всегда работай в ветке `claude/telegram-master-finder-bot-XXXXX`
- ✅ После КАЖДОГО push отправляй ссылку на PR пользователю
- ❌ НИКОГДА не пуши в `main` напрямую
- ❌ НИКОГДА не используй `git push --force` на main/master

### CODE:
- ✅ Используй `dict(row)` для sqlite3.Row / psycopg2.DictRow
- ✅ Проверяй совместимость PostgreSQL и SQLite
- ✅ Всегда используй `async/await` для telegram handlers
- ✅ Логируй важные события: `logger.info()`, `logger.error()`
- ❌ Не добавляй features без запроса пользователя
- ❌ Не меняй логику без понимания контекста

### NOTIFICATIONS:
- ✅ Delete old → Send new (для звука!)
- ✅ Обновляй счетчики в worker_notifications/client_notifications
- ✅ Обнуляй счетчики при просмотре

---

## 🚀 Быстрый старт для новой сессии

```bash
# 1. Проверь текущую ветку
git status

# 2. Посмотри последние коммиты
git log --oneline -5

# 3. Прочитай последние изменения
git diff HEAD~1

# 4. Начинай работу!
```

---

## 📝 Шаблон для начала новой беседы

**Копируй это в начало новой сессии:**

```
Проект: Telegram Bot для поиска мастеров (remontbot/telegram-bot-test)

Технологии: Python 3.11, python-telegram-bot v21.0, PostgreSQL/SQLite
Основные файлы: handlers.py (~10k), db.py (~6k), bot.py

Текущая ветка: claude/telegram-master-finder-bot-XXXXX
Main branch: main

Правила:
- Работаю ТОЛЬКО в ветке claude/telegram-master-finder-bot-*
- После каждого push отправляю ссылку на PR
- Коммиты на русском с подробным описанием

Важно:
- Двойная совместимость БД (SQLite + PostgreSQL)
- Async/await для всех handlers
- Smart notifications (delete + send для звука)
- Бейджи в меню с счетчиками

Готов к работе! 🚀
```

---

## 📞 Контакты и помощь

При возникновении вопросов:
1. Прочитай этот файл (`PROJECT_BRIEF.md`)
2. Проверь последние коммиты: `git log --oneline -10`
3. Поищи похожий код в handlers.py через grep
4. Спроси пользователя если не уверен

---

**Последнее обновление:** 2025-12-24

**Статус проекта:** ✅ Active Development
**Текущая версия:** 2.0 (Smart Notifications + Decline Orders)
